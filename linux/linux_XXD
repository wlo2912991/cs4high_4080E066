
# XXD 16進位編碼
```
xxd [options] [infile [outfile]]
xxd -r[evert] [options] [infile [outfile]]

xxd 命令用於用二進製或十六進制顯示文件的內容，如果沒有指定outfile參數，則把結果顯示在屏幕上，
如果指定了outfile則把結果輸出到 outfile中；如果infile參數為 – 或則沒有指定infile參數，則默認從標準輸入讀入。

常用選項：

-b

轉到 比特(二進制 數字) 模式, 而不是十六進制模式。在這種模式下, 每個字符被表示成八個 0/1 的數字，
而不是一般的十六進制形式。每一行都以一個用十六進制形式表示的行號，後面是ascii (或者 ebcdic) 形式開頭。命令行選項 -r, -p 在這個模式下不起作用。

-h

顯示幫助信息後退出。

-l N

只輸出 N 個字符。

-p

以 postscript的連續十六進制轉儲輸出，這也叫做純十六進制轉儲。

-r

逆向操作: 把xxd的十六進制輸出內容轉換回原文件的二進制內容。

-seek offset

用在 -r 之後: 會在當前檔桉的 偏移量上增加 .

-s [+][-]seek

從infile的絕對或者相對偏移量開始. + 表示相對於標輸入當前的位置(如果不標準輸入就沒有意義了). – 表示從檔桉末尾(如果和+ 連用: 從標準輸入當前位置) 向前數一些字符, 從那個地方開始. 如果沒有-s 選項, xxd 從當前位置開始.



-u

十六進制輸出時使用大寫字母，默認是小寫字母。
```

# 範例
```
lab@29d4cf5af0ab:~$ xxd -r -p hex.txt
BreakALLCTF{GIUWO1rpMhJCJpACvVwr}
```
